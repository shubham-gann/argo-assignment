apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: guestbook
spec:
  generators:
  - list:
      elements:
      - cluster: dev
        url: https://kubernetes.default.svc
      # Adding new cluster here Ashwin
      # - cluster: prod
      #   url: https://2.4.6.8
  template:
    metadata:
      name: '{{cluster}}-guestbook'
    spec:
      project: default
      source:
        repoURL: https://github.com/shubham-gann/argo-assignment.git
        targetRevision: HEAD
        path: app-set/{{cluster}}
        # create two folders with cluster name prod and dev. this generator will use them
      destination:
        server: '{{url}}'
        namespace: default
      syncPolicy:
        automated:
          prune: true
          selfHeal: true